import type { Config } from "tailwindcss";

export default {
  darkMode: ["class"],
  content: [
    "./pages/**/*.{ts,tsx}",
    "./components/**/*.{ts,tsx}",
    "./app/**/*.{ts,tsx}",
    "./src/**/*.{ts,tsx}",
  ],
  prefix: "",
  theme: {
    container: {
      center: true,
      padding: "2rem",
      screens: {
        "2xl": "1400px",
      },
    },
    extend: {
      colors: {
        border: "hsl(var(--border))",
        input: "hsl(var(--input))",
        ring: "hsl(var(--ring))",
        background: "hsl(var(--background))",
        foreground: "hsl(var(--foreground))",
        primary: {
          DEFAULT: "#1e40af", // Deep Trust Blue
          light: "#eff6ff",   // Light blue background
          dark: "#1e3a8a",    // Darker blue for hovers
        },
        secondary: {
          DEFAULT: "#059669", // Fresh Clean Green
          light: "#ecfdf5",   // Light green background
          dark: "#047857",    // Darker green for hovers
        },
        accent: {
          DEFAULT: "#f59e0b", // Energy Yellow
          light: "#fef3c7",   // Light yellow background
          dark: "#d97706",    // Darker yellow for hovers
        },
      },
      keyframes: {
        "fade-up": {
          "0%": {
            opacity: "0",
            transform: "translateY(10px)",
          },
          "100%": {
            opacity: "1",
            transform: "translateY(0)",
          },
        },
      },
      animation: {
        "fade-up": "fade-up 0.5s ease-out",
      },
    },
  },
  plugins: [require("tailwindcss-animate")],
} satisfies Config;